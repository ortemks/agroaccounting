const express = require('express');
const router = express.Router();

const URL = `/remains`
const RemainModel = require('../../accounting_models/accounting-types_models/remain');

module.exports = router